<!DOCTYPE html>
<html>
<head>
  <title>Work</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="container">

<nav class="navbar navbar-default">
  <div class="navbar-header">
    <a class="navbar-brand">在庫管理システム</a>
  </div>
  <ul class="nav navbar-nav pull-right">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        テーブル一覧<b class="caret"></b>
      </a>
      <ul class="dropdown-menu">
        <% @database_tables.each do |table_name, item| %>
          <li><%= link_to "#{item[:name]} (#{table_name})", :controller => "crud", :action => "index", :table => table_name %></li>
        <% end %>
      </ul>
    </li>
  </ul>
</nav>

<% flash.each do |name, msg| %>
  <div class="alert alert-<%= name == "notice" ? "success" : "error" %>">
    <a class="close" data-dismiss="alert">×</a> <!-- アラートダイアログに×ボタンをつける -->
    <%= msg %>
  </div>
<% end %>

<%= yield %>

<% console %>

</body>
</html>
